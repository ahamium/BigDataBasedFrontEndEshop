import { Platform } from "react-native";

//var baseURL = "";

var baseURL = "https://bigdatabasedbackendeshop.herokuapp.com/api/v1/";

/*
{
  Platform.OS == "android"
    ? (baseURL = "https://bigdatabasedbackendeshop.herokuapp.com/api/v1/")
    : (baseURL = "http://localhost:3000/api/v1/");
}
*/
export default baseURL;
